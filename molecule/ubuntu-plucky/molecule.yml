---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu-plucky
    image: ubuntu:25.04
    pre_build_image: true
provisioner:
  name: ansible
  config_options:
    defaults:
      ansible_become: false
  playbooks:
    converge: ../common/converge.yml
    prepare: ../common/docker_prepare.yml
    verify: ../default/verify.yml
verifier:
  name: ansible
scenario:
  name: ubuntu-plucky
  test_sequence:
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy
