---
# Example inventory for lab or hardware testing. Copy outside the repo and replace placeholders.
all:
  hosts:
    regolith-test-01:
      ansible_host: REPLACE_HOST_IP
  vars:
    ansible_user: ubuntu
    ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_private_key_file: REPLACE_SSH_KEY_PATH
